# Fingerprint first (succeeds without a password when a finger matches),
# falling back to the account password. Used by the Quickshell lock screen.
auth sufficient pam_fprintd.so
auth required pam_unix.so
