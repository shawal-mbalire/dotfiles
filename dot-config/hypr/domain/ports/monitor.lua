-- Port: MonitorPort — apply a Display model to an output

local M = {}
M.NAME = "MonitorPort"

function M.apply_monitor(display)
  error("MonitorPort.apply_monitor is not implemented")
end

return M
