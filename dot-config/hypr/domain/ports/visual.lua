-- Port: VisualPort — visual config sections, curves and animations

local M = {}
M.NAME = "VisualPort"

function M.configure_visual(section)
  error("VisualPort.configure_visual is not implemented")
end

function M.register_curve(bezier)
  error("VisualPort.register_curve is not implemented")
end

function M.register_animation(anim)
  error("VisualPort.register_animation is not implemented")
end

return M
