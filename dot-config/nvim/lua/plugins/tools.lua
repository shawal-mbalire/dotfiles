-- Tools Plugins
-- For utility plugins that don't fit into a specific domain category.
-- To add a new tool plugin:
--   1. Add preference to the relevant domain module
--   2. Create adapter in lua/adapters/<plugin>.lua
--   3. Add adapter.spec() call below

local specs = {}

return specs
